@page "/Name/{Value}"
<h3>Name</h3>

@code {
    [Parameter]
    public string Value { get; set; }
    
    
    protected override void OnParametersSet()
    {
    //the param will be set now
        var id = Value;
    }
}